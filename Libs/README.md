Put the compiled EveParser.Core.dll in this folder.

The GUI references this DLL directly, so it can be replaced with a newer build
without adding the EveParser.Core source project to this repository.
